<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Mentoring">

    <insert id="upload" parameterType="hello.example.porthub.domain.MentoringDto">
        insert into Mentoring(MentoID, Title, Content, Price, Thumbnail, file_urls)
        values (#{MentoID}, #{Title}, #{contents}, #{Price}, #{Thumbnailurl}, #{fileurls})
    </insert>

</mapper>